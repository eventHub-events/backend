export enum EventStatus {
  Draft = "draft",
  Upcoming = "upcoming",
  Active = "active",
  Completed = "completed",
  Cancelled = "cancelled",
  Flagged = "flagged",
}

export  enum EventType {
  Conference  = 'conference',
  Workshop   = "workshop",
   Sports   = "sports",
   Other  = "other"
}

export enum TicketStatus {
  Active = "Active",
  Inactive = "Inactive"
}